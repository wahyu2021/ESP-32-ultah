# Implementation Plan: Device Polling API

- [ ] Buat file `src/controllers/deviceController.ts`.
- [ ] Buat fungsi `handleDevicePoll` di dalam controller.
- [ ] Buat file `src/routes/deviceRoutes.ts`.
- [ ] Daftarkan endpoint `GET /poll` ke dalam `deviceRoutes`.
- [ ] Daftarkan `deviceRoutes` di `src/app.ts` pada prefix `/api/v1/device`.
